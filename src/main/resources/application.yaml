spring:
  profiles:
    active: prod
  jpa:
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
    database-platform: org.hibernate.dialect.PostgreSQL9Dialect
  upload-file-location: /opt/gms/storage/
  send-mail: true
logging:
  level:
    org.codealpha: info
  file: gms.log
---

spring:
  profiles: qa
  upload-file-location: /Users/apple/storage/
  send-mail: false
  datasource:
    url: jdbc:postgresql://localhost:5432/anudan
    username: apple
    password:
    initialization-mode: always
    platform: pg
  jpa:
    show-sql: true
    database: postgresql
    hibernate:
      jdbc:
        lob:
          non_contextual_creation: true
      ddl-auto: create
  jackson:
    default-property-inclusion: non_null
  mail:
    host: smtp.gmail.com
    port: 587
    username: ranjit.victor@enstratify.com
    password: enstratify123$
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
server:
  servlet:
    context-path: /api
---
spring:
  profiles: prod
  datasource:
    url: jdbc:postgresql://localhost:5432/gms
    username: postgres
    password: SA#1029
    initialization-mode: always
    platform: pg
  jpa:
    show-sql: true
    database: postgresql
    hibernate:
      jdbc:
        lob:
          non_contextual_creation: true
      ddl-auto: create
  jackson:
    default-property-inclusion: non_null
  mail:
    host: smtp.gmail.com
    port: 587
    username: ranjit.victor@enstratify.com
    password: enstratify123$
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
server:
  servlet:
    context-path: /api
